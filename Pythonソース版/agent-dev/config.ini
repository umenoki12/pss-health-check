[General]
; PCのID（空欄の場合は自動でPC名が取得されます）
PC_ID = 
; 送信間隔（秒）
UPDATE_INTERVAL = 60

; --- 🛡️ 新規追加：セキュア通信設定 ---
; ローカルテスト用のURL（本番環境に置くときは https://... に変えます）
SERVER_URL = http://127.0.0.1:5000
; サーバー（app.py）で設定したエージェント用合言葉
AGENT_TOKEN = AgentPass_XYZ
; ------------------------------------

[Monitor]
; 監視モード (PC または SERVER)
TYPE = PC
; 監視対象 (カンマ区切り)
TARGET_NAMES = test_app.exe, another_app.py